import { StyleSheet } from 'react-native';
import { COLOR } from '../../Containers/Login/styles';

const styles = StyleSheet.create({
  container: {
   
    position: 'relative',
    
  },
  header:{
   
    backgroundColor:'white'
  }
});

export default styles;